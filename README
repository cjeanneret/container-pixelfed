# Pixelfed Container
Just a "light" pixelfed container, based on Alpine Linux instead of heavy Ubuntu.

## Usage
podman run -d jinternux/pixelfed

## Listener
The listener is set to /run/pixelfed.sock instead of :9000
